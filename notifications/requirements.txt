# SafeCycle Sofia — Python dependencies
# Install with: pip install -r requirements.txt

# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Async Redis client (provides redis.asyncio)
redis>=5.0.0

# Pydantic v2 (FastAPI uses it internally too)
pydantic>=2.7.0

# Geospatial / routing
osmnx>=1.9.0
networkx>=3.3
shapely>=2.0.0
geopandas>=0.14.0
numpy>=1.26.0

# PostgreSQL async driver
asyncpg>=0.29.0
sqlalchemy[asyncio]>=2.0.0
psycopg2-binary>=2.9.0    # for sync tooling / migrations

# Firebase push notifications
firebase-admin>=6.5.0

# WebSocket support (included with uvicorn[standard] but explicit is better)
websockets>=12.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0             # needed by FastAPI TestClient for WebSocket tests